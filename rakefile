require_relative 'skanky_redis'
task :parallel do
  with_redis do |redis_connection_string|
    require 'pry'; binding.pry
    system("bundle exec parallel_cucumber --queue-connection-param #{redis_connection_string} -n 10" )
  end
end